# GUI (основной интерфейс)
PyQt6>=6.6

# Сборка .exe (опционально)
pyinstaller>=6.0

# Запасной интерфейс на Tkinter входит в стандартную библиотеку Python.
# ИИ-классификация (DeepSeek) использует только стандартную urllib — доп. пакетов не нужно.
