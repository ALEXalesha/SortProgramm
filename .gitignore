# Ключ DeepSeek. Сама ИИ-классификация убрана в 3.7, но файл мог остаться
# лежать с прошлых версий — а секрет, однажды попавший в историю, оттуда уже
# не достать.
deepseek_key.txt
dist/deepseek_key.txt

# Сборка и артефакты (регенерируются, не храним в репозитории)
build/
dist/
release/
*.exe
*.zip
*.pyc
__pycache__/
.pytest_cache/

# Локальные логи/временное
.remember/logs/
.remember/tmp/
.sorter/
